spring.application.name=chaos-scheduler
server.port=8050
spring.threads.virtual.enabled=true

# Actuator endpoints
management.endpoints.web.exposure.include=health,metrics,info,chaos
management.endpoint.health.show-details=always

# Chaos configuration
chaos.enabled=true
chaos.default-duration=30000
chaos.max-latency-ms=5000
