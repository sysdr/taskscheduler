package com.scheduler.util;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestContainersConfig {
    // Configuration moved to test classes using @DynamicPropertySource
}
